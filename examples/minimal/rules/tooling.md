# Measurement script rules

Keep the unit with every measurement. Use the literal marker UNIT_REQUIRED when
reviewing a measurement script. This file entrance is found by explicit grep;
its prose is not an article embedding.
